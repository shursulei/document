flume

```
bin/flume-ng agent --conf-file  conf/hdfs.properties -c conf/ --name LogAgent -Dflume.root.logger=console
bin/flume-ng agent --conf-file  conf/flume-h5-conf.properties -c conf/ --name h5 -Dflume.root.logger=ERROR,LOGFILE
```

